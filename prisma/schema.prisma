generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id       String  @id @default(uuid())
  npm      String  @unique
  name     String
  prodi    String
  password String
  token    String? @db.Text
}
